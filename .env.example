# Domain configuration for Traefik
DOMAIN=gazetteer.ukrytoemore.ru

# Traefik network name (should match your existing Traefik setup)
TRAEFIK_NETWORK=traefik-network

# Let's Encrypt certificate resolver (should match your Traefik configuration)
CERT_RESOLVER=letsencrypt
